
Implement the approved plan for GitHub issue $ARGUMENTS.

- If you need to read a file that hasn't already been read in this conversation, use a Haiku agent to fetch and summarize only the relevant section — do not read the full file inline.
- Run `npm run build` in the background when all changes are complete. Do not wait for it to finish before reporting that implementation is done.
- Report what you changed and which files were modified. The build result will follow asynchronously.
