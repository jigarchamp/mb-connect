
Review all open GitHub issues for this project. Identify the next 10 issues to address in a sprint, in priority order.

Write the results to `backlog.txt` in the project root (overwriting the existing file) with the following format for each issue:
- Priority number (1–10)
- Issue number and title
- A clear explanation of why it is a priority for this sprint (consider: is it a bug blocking current functionality, a core MVP feature, a dependency for other issues, or a UX improvement for alpha testing?)
- Any questions that need to be answered before the issue can be implemented in code (include these in the file — they will be addressed when we tackle the issue)

Also include a short "housekeeping" section at the top listing any open issues that appear to already be implemented and should be closed.

End the file with a 2–3 sentence summary of what the sprint delivers as a whole.